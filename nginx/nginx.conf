server {
    listen 80;
    server_name 1tsme.site; # 등록한 도메인
    charset utf-8;
    # Allow only for register SSL (Certbot)
    location /.well-known/acme-challenge/ {
        allow all;
        root /var/www/certbot;
    }
    # 백엔드, 프론트엔드 연결 생략
}
